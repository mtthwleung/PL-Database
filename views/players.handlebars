{{#each teamPlayers}}
  {{#if @first}}
    <h1 class="teamName">{{name}}</h1>
  {{/if}}
{{/each}}

<table>
    <thead>
        <th scope ="col"><strong>Player Name</strong></th>
        <th scope ="col"><strong>Position</strong></th>
        <th scope ="col"><strong>Market Value (€)</strong></th>
    </thead>
    <tbody>
        {{#each teamPlayers}}
            <tr>
                <td>{{this.player_name}}</td>
                <td>{{this.position}}</td>
                <td>{{this.market_value}},000,000</td>
            </tr>
        {{/each}}
    </tbody>
</table>